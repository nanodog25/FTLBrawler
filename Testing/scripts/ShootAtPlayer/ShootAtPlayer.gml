if (_lane == global.playerLane && sign(x - global.playerX) == -_direction)
{
	ai_fire = true;
	//need to check for obstacles!
	//need to point everything at InSeconds
	//need to only change image_xscale once to match direction
}